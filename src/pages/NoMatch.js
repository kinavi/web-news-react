import React from 'react';
import {Page} from '../components/Page';

const NoMatch = ({route}) =>{
  return (
    <Page>
            404
    </Page>

  );
};

export default {
  component: NoMatch,
};
